import React from "react";
import { View, Text } from "react-native";

const CardList = ({ navigation: { navigate } }) => {
  return (
    <View>
      <Text>CardList</Text>
    </View>
  );
};

export default CardList;
