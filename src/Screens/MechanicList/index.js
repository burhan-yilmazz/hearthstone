import React from "react";
import { View, Text } from "react-native";

const MeachanicList = ({ navigation: { navigate } }) => {
  return (
    <View>
      <Text>MeachanicList</Text>
    </View>
  );
};

export default MeachanicList;
