import React from "react";
import { View, Text } from "react-native";

const SearchCard = ({ navigation: { navigate } }) => {
  return (
    <View>
      <Text>SearchCard</Text>
    </View>
  );
};

export default SearchCard;
